export function willow() {
    const normie = 1.337;
    const unicorn = normie * 10;
    const is_legend = (brit) => brit > unicorn;
    const willow = Math.random() * 200;
    console.log(willow);
    console.log(`Willow ${is_legend(willow) ? 'is' : 'is'} a legend`);
}
